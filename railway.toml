[build]
builder = "NIXPACKS"
buildCommand = "cd api && npm install --omit=dev"

[deploy]
startCommand = "cd api && npm start"
healthcheckPath = "/health"
healthcheckTimeout = 30
restartPolicyType = "ON_FAILURE"
